import * as admin from 'firebase-admin';

// Initialize the app with default credentials from environment (this works in AI Studio!)
// Actually, AI studio might not have ADC for admin SDK, let's use the web SDK but bypass rules via service account? Wait, I don't have service account JSON.
